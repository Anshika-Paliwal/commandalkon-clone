import React from "react";

const Button = (props) => {
  return (
    <button className="sm:text-xs text-lg hover:bg-red-500 border-2 hover:border-red-500 sm:text-white px-6 py-2 rounded-full transition-all duration-200 ease-in-out">
      CONTACT
    </button>
  );
};

export default Button;
